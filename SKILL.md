---
name: dream-iea
description: "Dream-IEA —— 基于信息元素回溯法（IEA）的梦境分析系统。三层映射：信息元素 → 意象图式 → 心理状态。触发词：「解梦」「梦境分析」「做梦」「梦到」「梦境日记」「dream」「iea」「意象图式」"
---

# Dream-IEA · 梦境意象元素分析

> 梦境叙事 → 信息元素 → 意象图式 → 心理状态。绝不跳过中间层做象征翻译。

## 何时触发

用户提到以下关键词时加载本技能：

**分析词：** 解梦、梦境分析、梦的 meaning、梦代表什么、分析梦境
**记录词：** 梦境日记、记录梦、做了个梦、梦到
**方法词：** IEA、信息元素、意象图式、图式频率、Gestalt 闭合
**系统词：** dream-iea、Dream-IEA、三层映射

## 核心流程（三层映射 · 不可跳步）

```
梦境叙事（碎片化 or 完整均可）
  ↓ Step 1: 元素解码       → 信息元素（客观标注，不解释）
  ↓ Step 2a: 图式识别      → 意象图式（客观模式检测，不推断）
  ↓ Step 2b: 理论翻译      → 心理状态（心理学/神经科学驱动）
  ↓ Step 3: 三线叠图       → 锚定到个人时空框架
```

**铁律**：信息元素不直接指向心理状态。必须经过意象图式中层。绝不做「梦到 X = 你在 Y」的象征跳译。

## 操作步骤

### Step 0：个人感觉景观校准

建立梦者的「感觉基线」——提问获取：近期 1-2 周生活状态、压力事件、长期情绪基调、创伤史（自愿）、文化背景。

### Step 1：元素解码

逐篇标注七类信息元素，**只做识别不做解释**。碎片化输入直接映射；完整叙事先去叙事化再拆解。

**NRC VAD 词级情感扫描**（可选，推荐）：对梦境原文运行 `vad_lookup.py` 获得客观的 V/A/D 聚合画像和主导情绪归类。替代分析师手动判断情绪类型，提高可复现性。

```bash
python scripts/vad_lookup.py "梦境文本"
python scripts/vad_lookup.py -i dream.txt --json
```

详见 `references/element_typology.md`。

### Step 2a：图式识别（客观层）

运行脚本或手动统计：
- 图式频率（对比基线）
- 图式共现聚类
- 情感分布（全谱，正负对称检测）
- Gestalt 闭合检测
- 日间残留检测（区分当天回放 vs 深层模式）

```bash
python scripts/analyze_dreams.py -i <diary.json> [--C2 N --C3 N --C4 N] [-o report.txt]
```

脚本自动计算 C1/S1/S2/S3；C2/C3/C4 由分析者赋值（也可内嵌 JSON 顶层 analyst 段）。

### Step 2b：理论翻译（推断层）

对每个闭合图式模式，基于形成机制翻译：体验域映射 → 体验域合成 → VAD匹配（emotion-D与schema-D一致性检查，背离时触发D张力调制）→ 语境填充。所有模式翻译完成后，执行收敛检查（4信号 + D张力辅助调制）。用追问句式交还梦者验证。

详见 `references/theory_translation.md`。

### Step 3：三线叠图

| 线 | 内容 | 来源 |
|----|------|------|
| 图式分布线 | Step 2a 输出 | 梦境标注 |
| 个人传记线 | 生命史关键节点/创伤/转折 | Step 0 + 用户补充 |
| 近期事件线 | 梦前 1-7 天事件时间线 | 每日配注 |

三线交会 = 心理状态推断锚点。

### 输出双面报告

报告分两部分，面向不同阅读需求：

- **第一部分（梦境画像）**：感性 · 可读性优先。叙事性语言呈现核心梦境主题，用"你"贯穿，不出现图式编号/光谱数值/评分条等任何技术标记。先让梦者"感觉对"。
- **第二部分（评分与解释）**：理性 · 分析性优先。图式激活数据 + 形成机制翻译链 + 收敛预检 + 趋势分析 + 光谱评分 + R规则 + 风险标记。每一步可追溯。

两部分基于同一套 Step 1→3 分析结果，不矛盾。审计方法：逐句检查第一部分的每个描述，在第二部分中都能找到对应的图式/情绪/趋势证据。

报告深度由日记数量决定：1篇→仅第一部分；2-6篇→第一部分+第二部分精简；≥7篇→完整双面；≥14篇→个人基线。

详见 `assets/report_template.md`。

## 输入兼容

碎片化（词组/短句/跳跃片段）和完整叙事（语法规范）同等对待，走同一解码路径。

## 约束与边界

- **不替代临床诊断**：不产出 DSM/ICD 标签。持续高强度负面图式模式 → 建议寻求专业帮助
- **需要梦者验证**：第二、三层推断必须经用户确认
- **日记数量下限**：至少 7 篇，理想 14 篇
- **置信度必须可追溯**：每个子维度分数 + 来源（脚本自动/分析者赋值）必须明示
- **综合指数为启发式**：权重基于专家判断而非经验校准，不提供统计可靠性保证
- **全情感谱覆盖**：不因「积极」降权（R7），不因「负面」放大

## 风险标记规则

以下模式出现时，明确建议寻求专业帮助（不自行诊断）：
- 反复 {FORCE + CONTAINER破裂 + 恐惧} 且不闭合（连续多日，强度不降）
- 情感分布中恐惧/悲伤占比持续 >50% 且无下降趋势
- 自我否定/消失类图式（同一性否定 + CENTER-PERIPHERY 极端边缘化）密集出现

措辞：「该图式模式显示高强度且未缓解的情绪负荷，建议考虑与心理健康专业人士交谈。」

## 文件清单

```
_Skills/dream-iea/
├── SKILL.md                          ← 本文件
├── references/
│   ├── element_typology.md           ← 七类元素 + 意象图式速查 + 情绪映射约定 + D贡献
│   ├── scoring.md                    ← 评分与置信度规范（对齐 LPP）+ D张力辅助调制
│   ├── theory_translation.md         ← 形成机制翻译规范 + VAD匹配 + D张力调制
│   └── knowledge_pack.md             ← [弃用·历史参考] 旧六理论框架，勿用于分析
├── scripts/
│   ├── analyze_dreams.py             ← Step 2a 图式层统计引擎
│   └── vad_lookup.py                 ← Step 1 NRC VAD 词级情感评分（中文）
├── data/
│   └── NRC-VAD-Chinese-Simplified.txt ← NRC VAD Lexicon v1.0 中文版（~20K词）
├── assets/
│   ├── dream_diary_template.md       ← 梦境采集模板
│   └── report_template.md            ← 双面报告格式（画像+评分）
└── examples/                          ← 用户梦境日记输入目录（样例数据已移除，放自己的 JSON）
```

## 诚实边界

- 本技能**不提供临床诊断**，不输出 DSM/ICD 标签
- 所有心理推断**必须交还梦者本人验证**，分析者不拥有最终解释权
- 单篇日记不足以建立基线，至少 7 篇，理想 14 篇
- 综合置信度指数为**启发式指标**，权重非经验校准

---

*版本：2026-07 迁移至 EDENOFLUX · 2026-07 理论翻译层重构为形成机制 · 2026-07 趋势效价修复+数据校验+术语统一 · 2026-07 报告重构为双面结构（对齐 LPP） · 2026-07 集成VAD情感维度框架（D张力调制+NRC词级评分） · 核心知识自包含*
